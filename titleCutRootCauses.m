RCA = {'OML Fault',...
    'NO CRC HEARTBEAT',...
    'COMBINER INPUT FAIL FORWARD BUS LINK LOSS',...
    'COMM LOST BETWEEN CBR AND CRC',...
    'COMM LOST BETWEEN TXAMP AND CRC',...
    'RECTIFIER FAIL',...
    'FUSE FAIL',...
    'COMM LOST BETWEEN TFU AND PBM',...
    'CRITICAL ALARM CIRCUIT FAILED',...
    'GPS FLYWHEELING INFO ERROR',...
    'RX SYNTHESIZER OUT OF LOCK',...
    'AMPLIFIER SHELF FAN FAILURE',...
    'DIGITAL SHELF FAN FAILURE',...
    'TX AMP LOSS',...
    'OVER TEMPERATURE'};